[pytest]
testpaths = tests
# Without this, bare `pytest` (as opposed to `python -m pytest`) doesn't add the repo
# root to sys.path, so `import kvittomall` in conftest.py/tests fails - `python -m`
# does this automatically, but CI and most people's muscle memory just run `pytest`.
pythonpath = .
